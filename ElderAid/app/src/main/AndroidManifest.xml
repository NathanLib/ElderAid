<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.ac.rgu.elderaid">


    <uses-sdk
        android:minSdkVersion = "28"
    android:targetSdkVersion = "29" />

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ContactAddActivity"></activity>
        <activity android:name=".ContactDetailsActivity" />
        <activity android:name=".TaskCheckListActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CalendarActivity" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".PrescriptionLevelActivity" />
        <activity android:name=".MedicalInfoActivity" />
        <activity android:name=".MapsEditFavouritesActivity" />
        <activity android:name=".PreferencesActivity"/>

    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>